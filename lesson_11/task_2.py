import random
random.seed(1)

bound = 100

print(round(random.random() * bound, 2))
print(round(random.random() * bound, 2))
print(round(random.random() * bound, 2))
print(round(random.random() * bound, 2))
